import pandas as pd

def extract_api_data(api_url: str) -> pd.DataFrame:
    """Template for future real API extraction."""
    raise NotImplementedError("Connect this function to ASX, weather or ABS/RBA APIs.")
